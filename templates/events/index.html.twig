{% extends 'base.html.twig' %}

{% block header %}

    <h1>{{events |length}} Events Pages</h1>


{{events |join(', ' ,' and ')}}
  <ul class="nav-link">
    {# <li>Symfony<li>
    <li>python<li>
    <li>django<li> #}
    {% for event in events %}
       <li>{{ event }}</li>
    {% endfor %}
  </ul>
{% endblock header %}